server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: "http://localhost:8761/eureka"
  instance:
    preferIpAddress: true

management:
  tracing:
    sampling:
      probability: 1.0